object HardcodedCredentials {
  // VULNERABLE: credentials in source code
  val DB_USER = "admin"
  val DB_PASS = "P@ssw0rd!"
  
  def connect(): Unit = {
    println(s"Connecting with $DB_USER / $DB_PASS")
    // pretend to connect...
  }
}
